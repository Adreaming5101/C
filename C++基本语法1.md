```c++
#include <stdio.h>
    /*
    stdio.h 系统库文件
    #include 预编译指令，导入系统库文件
    <直接搜索系统自带的库>
    ""先搜索用户自定义的库，再搜索系统库
    */


#include <stdio.h>    // 相当于python的import效果，包含输入输出scanf，printf功能等。
#include <stdlib.h> 
#include <math.h>     // 包含数学函数，sqrt，fabs等功能
int main() {
    int a = 5;
    double b = 3.14;

    printf("Square root of %d is %f\n", a, sqrt(a));
    printf("Absolute value of %f is %f\n", b, fabs(b));

    return 0;
}


// 字符与字符串
'1'    //表示字符，'123'会报错，单引号只能跟一个字符,单引号用来表示字符常量，只能包含一个字符。如果你尝试在单引号内放置多个字符，编译器通常会报错。
"123"  //表示字符串
    
   
//转义符---->\   (还有其他字符，如，\t \n 等，可自行补充)
printf("hello wo\"r\"ld")

    
// 预处理指令，使用 #define 来创建一个宏定义
#define NUMBER 9  
    /*
    每当你在代码中使用 NUMBER 这个标识符时，编译器会在编译代码之前将所有的 NUMBER 替换为 9。可以避免在代码的多个地方多次出现 9 这个数字，提高了代码的可读性和可维护性。
    */
#include <iostream>
#define NUMBER 9
int main() {
    printf("Hello World   %d",NUMBER);
    return 0;
}
   

// 变量的定义
    /*
    数据类型说明 + 标识符;
    */
int nSum;
short nNumber;   //short(整数变量) 变量占用 2 字节的内存空间，其范围一般为 -32,768 到 32,767
    

// 变量的初始化
    /*
    1.给定一个有意义的值
    2.清理内存中的垃圾值
    */
int nNum = 0;   // 清理内存中的垃圾值，变量的定义声明一块内存，在程序的执行过程中，这块内存可能被别人使用了，可能还有残留，所以赋值0来初始化，清理内存中的垃圾值
float fNum = 5.77;   // 给定一个有意义的值
// 变量赋值
nNum = 60         // 直接使用，不用再声明类型
fNum = 6.77

    
//全局变量和局部变量声明
    /*
    在函数外层声明全局变量，多个函数可以共享，也可修改，增加维护负担；局部在函数内部声明，只作用于内部
    */


 补充：
    #include <stdio.h> 标准库的常用其他函数：
    getchar():
    从标准输入中读取一个字符。
        
    putchar(int c):
    将一个字符输出到标准输出。
        
    **fgets(char str, int n, FILE stream):
    从指定的文件流读取一行字符串。
        
    **fputs(const char str, FILE stream):
    将字符串写入指定的文件流。
        
    *fclose(FILE stream):
    关闭文件流。
        
    **fopen(const char filename, const char mode):
    打开一个文件，并返回一个文件流。
        
    **fprintf(FILE stream, const char format, ...):
    格式化输出到指定文件流。
        
    **fscanf(FILE stream, const char format, ...):
    从指定文件流读取格式化的数据。
        
    *feof(FILE stream):
    检查文件流是否已到达文件末尾。
        
    *rewind(FILE stream):
    将文件流的位置重置到文件的开头。
    
```

声明变量所做的事情：

- ![avatar](picture\Snipaste_2024-01-09_11-41-49.png)

### 数据类型

```c++
size_t: 一种用于表示内存大小和对象大小的无符号整数类型，在不同系统中的位数可能会有所不同。

/*双精度与单精度浮点数*/
int main(){
    const double PI = 3.14159;     // const 定义一个常量，表示这个变量是常量，不能在后续的程序中修改。
    int n = 8;
    printf("%lf\n",PI*n*n);   // %lf用于格式化输出一个double类型的值。
    float fPoint = 5.0f / 2;    // 5.0 表示一个双精度浮点数，即 double 类型，5.0f 表示一个单精度浮点数，即 float 类型。在C语言中，默认情况下浮点数常量是双精度的，如果需要明确指定为单精度，可以使用 f 后缀。
    printf("%f",fPoint);
}
区别：
    精度：
        单精度（float）： 使用32位（4字节）存储，提供大约7位有效数字的精度。
        双精度（double）： 使用64位（8字节）存储，提供大约15到17位有效数字的精度。
    存储空间：
        单精度（float）： 占用较小的存储空间，适用于有限的存储资源或对精度要求不太高的场景。
        双精度（double）： 占用较大的存储空间，适用于对精度要求较高的科学计算、工程计算以及需要处理大范围数值的场景。
    使用场景：
        单精度（float）： 适用于需要节省存储空间、对精度要求不是很高的情况，例如图形处理、嵌入式系统等。
        双精度（double）： 适用于需要更高精度的科学计算、工程计算、金融计算等领域。

    
    
int 适用于一般的整数计算，通常足够表示一般范围的整数。
long 适用于较大范围的整数计算，通常用于需要超出 int 范围的情况。
long long 适用于需要非常大范围的整数，如大整数计算、时间戳表示等。
    // 内存占用：
    int 通常占用4个字节（32位系统）或8个字节（64位系统）。
    long 通常占用4个字节（32位系统）或8个字节（64位系统）。
    long long 通常占用8个字节
```

### 函数

```c++
// scanf函数的用法和使用场景
// 用法：可以接收用户输入的数据并将其存储到指定的变量中。这个函数可以读取各种类型的数据（整数、浮点数、字符等）。

​```c++
// 读取整数
#include <stdio.h>

int main() {
    int number;
    
    printf("请输入一个整数：");
    scanf("%d", &number);
    
    printf("你输入的整数是：%d\n", number);
    return 0;
}
/*
程序提示用户输入一个整数，然后使用 scanf 函数读取用户输入的整数，并将其存储到 number 变量中。%d 是格式化字符串，用来指示 scanf 应该读取一个整数。&number 是 number 变量的地址，scanf 使用 & 操作符来将输入的值存储到这个地址对应的变量中。
*/
// 读取字符串
/*
[50] 表示 name 这个数组的大小。在C语言中，这行代码声明了一个名为 name 的字符数组，其大小为 50。这意味着 name 这个数组可以存储最多 50 个字符。在C中，字符数组用于存储字符串，字符串以 null 结尾（即以 '\0' 结束）。所以，char name[50]; 声明了一个可以存储长度最多为 49 个字符的字符串，因为最后一个位置需要放置 null 终止符。
*/
#include <stdio.h>

int main() {
    char name[50];     
    
    printf("请输入你的名字：");
    scanf("%s", name);
    
    printf("你好, %s!\n", name);
    
    return 0;
}
/*
程序提示用户输入一个字符串（名字），然后使用 scanf 函数读取用户输入的字符串，并将其存储到 name 数组中。%s 是格式化字符串，用来读取字符串。在这里，name 是一个字符数组，它会存储用户输入的字符串，数组不用&符号。

scanf作用和使用场景：
scanf 函数是一个用于从用户输入中读取数据的重要函数。它的作用是将用户输入的数据读取到程序中，这样程序就可以使用这些数据进行计算、显示或者进行其他操作。它可以用于各种需要用户交互输入的场景，例如创建交互式程序、从用户获取配置信息等。需要注意的是，scanf 对输入数据的格式有要求，如果输入的格式与 scanf 指定的格式不符，可能会导致意外行为或错误。
*/
​```

// getchar()函数
    /*
    用法：从标准输入获取一个字符，若输入有多个字符，默认获取第一个
    使用场景：
        字符处理：对于需要逐个字符处理的情况，比如需要逐个读取用户输入的字符来实现某种特定的逻辑。
        简单交互：用于简单的控制台输入交互，获取用户输入的字符来执行相应的操作。
        简单菜单：在实现简单的菜单选择功能时，可以使用 getchar() 读取用户输入的选择。
        调试和测试：有时用于调试和测试目的，暂停程序执行，等待用户输入。
    */
	#include <stdio.h>

    int main() {
        char ch;

        printf("请输入一串字符，以回车结束（输入'q'退出）：\n");
        fflush(stdout); // **刷新输出缓冲区,立即输出，而不是等缓冲区满了或按换行符才输出到控制台**
        while ((ch = getchar()) != 'q') {   // **如果输入多个字符，会遍历每个字符来判断并执行逻辑，注意点：我们输入字符后会按回车键，也就是\n也会被输入到ch变量中，所以会多打印一次**
            printf("你输入的字符是：%c\n", ch);
            fflush(stdout); // 刷新输出缓冲区    **上一行代码打印后立即输出**
        }

        return 0;
    }


    #include <stdio.h>
	// 尽量不要用到ffluch(stdin)输入
    int main() {
        char ch;

        printf("请输入一串字符，以回车结束（输入'q'退出）：\n");
        fflush(stdout); // 刷新输出缓冲区

        while ((ch = getchar()) != 'q') {
            if (ch != '\n') { // **忽略换行符，防止输入字符后按回车，加入到ch里的回车符号**
                printf("你输入的字符是：%c\n", ch);
            }
            fflush(stdout); // 刷新输出缓冲区
        }

        return 0;
    }






```

### 占位符

```C++
%c和%s:
	/*
	都是在 C 和 C++ 中用于格式化字符串的占位符。
	%c 用于处理单个字符，在输入时，%c 从输入流中读取一个字符；在输出时，%c 将一个字符打印到输出流中。
	%s 用于处理字符串。在输入时，%s 从输入流中读取一串字符，直到遇到空格、制表符或换行符等空白字符为止，并将这些字符存储到一个以 null 结尾的字符数组中；在输出时，%s 打印该字符数组中的内容，直到遇到 null 终止符为止。
	区别和总结：
	%c 用于处理单个字符，而 %s 用于处理字符串（字符序列）。
	%c 只能处理一个字符，包括字母、数字、特殊字符等；%s 则可以处理一串字符，直到遇到空白字符或者字符串结尾。
	%c 在读取时不会跳过空白字符，而 %s 在读取时会跳过前导空白字符并且遇到空白字符会停止读取。
	%s 在处理字符串时需要使用字符数组来存储字符串，字符串以 null 结尾，而 %c 只是单个字符的处理，通常存储在一个字符变量中。
	*/

%d:

%f:

%zu: 用来格式化输出 size_t 类型的值的格式控制符。sizeof占位符

%lf：用于格式化输出一个double类型的值。
    
%p：用于打印指针地址，如：
    #include <stdio.h>
    int main () {
        int a = 10;
        printf("%p", &a);   // &运算符取址-----16进制地址
    }

%ld：用于打印长整数（long integer）类型的值。
```

### 运算符

* \*/ %(取模，取余)：他们的优先级一样，因为小数是除不尽的，所以取余两侧必须都是整形

##### 自增与自检运算符

```c++
自增运算符与自减运算符：
    //自增自检运算符只能作用于整形变量以及字符型变量，不能作用于常量或运算表达式：如，5++，(a+b)++
    a++  <==> a = a + 1
    a--  <==> a = a - 1
    // a++、a-- 处理不了a + 2等情况，只能是a+1,a-1

    ++a 与 a++区别(自增运算符，a--  --a为自减运算符)：
        ++a 先自加，后参与运算;
        a++ 先参与运算，后自加;
    #include <stdio.h>
    int main() {
        int a = 8;
        int b = 0;
        b = 3 + a++;
        printf("%d\n", b); // b=11,a=9

        b = 3 + ++a;
        printf("%d", b);   // b=13,a=10
    }

    #include <stdio.h>
    int main() {
        int i = 5;
        int j;
        j = i++;
        printf("i=%d,j=%d\n", i, j);  // i=6,j=5
        printf("i=%d,j=%d\n", i++,j++);  // i=6,j=5，打印之后加
        printf("i=%d,j=%d", i, j);  // i=7,j=6
    }

```

##### 关系运算符

```c++
// >   <   >=    <=   ==  !=

/*
对于浮点型，只能比较大小，要比较是否相等需要精确小数位

关系运算符的返回值为布尔类型，结果不是0就是1

C++如何精确小数位？
*/

```

##### 逻辑运算符

```c++
逻辑与：&&
逻辑或：||
逻辑非：!
一般形式：表达式 逻辑运算符 表达式
逻辑非的优先级高于算数运算符 ：
    int a = 5;
	int b = 7;
	int c;
	c = a + !b   // 5 + !7 = 5  7为真，非7为0


短路运算：
       逻辑与： 第一个表达式为假，不需要再运算第二个表达式，结果为假
       逻辑或： 第一个表达式为真，不需要再运算第二个表达式，结果为真
```

##### 逗号运算符

```c++
逗号运算符(又称顺序求值运算符)：可以把表达式连接起来，整个逗号表达式的值为最后一个表达式的值，逗号运算符的优先级是所有运算符中最低的。如，
    #include <stdio.h>
    int main() {
        int a = 3;
        a = 3*5,a*4;
        printf("%d", (3*5,a*4));  // 表达式的值为60
        return 0;
    }
```

##### sizeof()运算符

```c++
用来获取数据类型大小的运算符。它返回指定类型或变量的字节数。
    1.用于数据类型
   	  sizeof使用形式：sizeof(int)
      数据类型必须用括号括住
    2.用于变量
      使用形式：sizeof(a)
sizeof操作符不能用于函数类型，不完全类型（未知存储大小的数据类型）或位字段。void

sizeof()直接参与运算：--------------->sizeof(xx) 返回的是总字节数
    int i = 10;
	i * sizeof(int)
        
使用场景：
内存分配： 在动态内存分配时，可以使用 sizeof 来确定分配内存的大小，特别是在使用 malloc 或 calloc 分配内存时，以确保分配足够的空间。
数组操作： 对数组进行操作时，sizeof 可以帮助计算数组元素的个数，这对于循环或者动态数组的操作非常有用。
跨平台编程： 在编写可移植的代码时，使用 sizeof 可以确保数据类型在不同平台上具有相同的大小。
   	int *ptr;
	ptr = (int *)malloc(5 * sizeof(int)); // 分配足够空间来存储5个整数


/*
 初始化char int float double 类型的四个变量a,b,c,d 计算其在内存中所占有的空间大小(sizeof())
*/

#include <stdio.h>
int main() {
    char a;
    int b;
    float c;
    double d;
    printf("%zu \n",sizeof(a));
    printf("%zu \n",sizeof (b));
    printf("%zu \n",sizeof (c));
    printf("%zu \n",sizeof(d));
    fflush(stdout);
    return 0;
}
    
```

##### 运算符的优先级与结合性

```c++
由低到高分别是：
    逗号运算符 < 赋值运算符 < 条件运算符 < 逻辑运算符 < 位运算符 < 关系运算符 < 算数运算符 < 特殊运算符

大多数运算符从左往右执行，例如：
    a++, a-b+c, a%b, a>=b

    
右结合性(从右往左执行)：
    a = b = c = 5    // 将5赋值给c再赋值给b再赋值给a
	前增量：++a
    前减量：--a
    逻辑非：!
    正负号
    取地址：&
    sizeof
    
补充：
    // 找优先级最低的，为表达式名称
    a = b = c =5    //赋值表达式 
    b + c,c + d     //逗号表达式
    a > b || c      //逻辑表达式
    a > b + c       // 关系表达式
    a + b           //算数表达式
    
    
    int a,b,c,d;
	int e = (a=1,b=a++,c=a*b,d=c++ +b);
	int f = (a=1,b=++a,c=a*b,d=c+ ++b);
```

**各种运算符说明：**

```c++
逗号运算符 ,：
用于在表达式中使用多个子表达式，按顺序执行，并返回最后一个表达式的值。
示例：int x = (2, 3); // x的值为3

赋值运算符 =：
用于将右侧表达式的值赋给左侧的变量。
示例：int a = 10;

条件运算符 ? :：
用于根据条件的真假选择不同的值。
示例：int x = (a > b) ? a : b;

逻辑运算符 &&（逻辑与）、||（逻辑或）、!（逻辑非）：
用于进行逻辑运算，返回布尔值。
示例：if (x > 0 && y < 10)
    
位运算符 &（按位与）、|（按位或）、^（按位异或）、~（按位取反）、<<（左移）、>>（右移）：
用于对二进制位进行操作。
示例：int result = a & b;

关系运算符 ==、!=、<、>、<=、>=：
用于比较两个表达式的关系。
示例：if (x == y)
    
算数运算符 +、-、*、/、%：
用于进行基本的算术运算。
示例：int sum = a + b;

```



##### 类型转换

```c++
/*
当不同类型的数据进行计算时，应当先将操作数转换成相同类型数据，然后再进行计算。
为了保证数据的完整性，一般低精度向高精度转换。
转换形式分为：隐式类型转换，显示类型转换。
C语言规定转换规则是低精度向高精度转换。
无论是强制转换或自动转换，都只是为了本次运算的需要而对变量的数据长度进行的临时性转换，而不改变数据说明时对该变量定义的类型。
*/

注意点：赋值运算时如两边类型不同，将自动转换为和左边相同的类型，如：
    a = (int)7.5    // 7

    

```

**隐式类型转换：**

![avatar](picture\Snipaste_2024-01-08_21-39-44.jpg)

**显示类型转换：**

![avatar](picture\Snipaste_2024-01-08_21-40-03.jpg)













